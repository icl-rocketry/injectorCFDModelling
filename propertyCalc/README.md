Uses coolprop & equations from RIT document to return saturation properties at any temperature. 
Compiled to .exe to hopefully run without required libraries/python version.
To build to .exe use:
    pyinstaller -wF genSatPropsGUI.py
